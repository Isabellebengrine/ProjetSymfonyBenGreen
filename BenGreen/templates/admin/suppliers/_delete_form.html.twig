<form method="post" action="{{ path('admin_suppliers_delete', {'id': supplier.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer ce fournisseur?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ supplier.id) }}">
    <button class="btn btn-warning">Supprimer</button>
</form>
