<form method="post" action="{{ path('admin_totalorders_delete', {'id': totalorder.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) de vouloir supprimer cette commande?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ totalorder.id) }}">
    <button class="btn btn-warning">Supprimer</button>
</form>
