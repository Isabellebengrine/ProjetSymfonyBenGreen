<form method="post" action="{{ path('totalorder_delete', {'id': totalorder.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ totalorder.id) }}">
    <button class="btn">Delete</button>
</form>
