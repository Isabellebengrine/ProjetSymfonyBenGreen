{% extends 'base.html.twig' %}

{% block title %}Modifier un Produit{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12 col-sm-12 d-flex justify-content-between align-content-center my-4">
            <h1 class="text-info">Modifier ce produit</h1>
            <a href="{{ path('products_index') }}">
                <button class="btn btn-info">Retour à nos produits</button>
            </a>
        </div>

    </div>

    {{ include('products/_form.html.twig', {'button_label': 'Modifier'}) }}

    {{ include('products/_delete_form.html.twig') }}
{% endblock %}
