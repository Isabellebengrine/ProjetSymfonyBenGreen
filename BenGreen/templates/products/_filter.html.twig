{{ form_start(form, {attr: {class: 'filter js-filter-form'}}) }}

    <div class="spinner-border js-loading" role="status" aria-hidden="true" style="display: none">
        <span class="sr-only">Loading...</span>
    </div>

    {{ form_row(form.q) }}
    <h4 class="text-info">Catégories :</h4>
    {{ form_row(form.rubriques) }}

    <h4 class="text-info">Prix :</h4>
    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.min) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.max) }}
        </div>
    </div>
    <div id="price-slider" data-min="{{ min }}" data-max="{{ max }}" style="margin-bottom: 3rem;">
    </div>

    <button type="submit" class="btn btn-info w-100">Filtrer</button>
{{ form_end(form) }}